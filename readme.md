# CSS Grid Demo Landing Project

```bash
npm i        # install dependencies
gulp         # build and run landing on local server (Ctrl+C to stop)
```

## What's inside?
The whole project including source files and boilerplate with Gulp, Sass, Autoprefixer and Uglify.

 [`View landing`](https://css-grid-landing-demo.netlify.com/)

