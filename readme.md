# CSS Grid Demo Landing

```bash
npm i             # install dependencies
gulp         # build and run landing on local server (Ctrl+C to stop)
```

## What's inside?
The whole project including source files, boilerplate with Gulp, Sass, Autoprefixer and Uglify.
